# ================================
# NUTRIFLOW FRONTEND ENVIRONMENT
# ================================

# Leave empty to use Vite proxy (works in both local and Codespaces)
# The proxy in vite.config.ts forwards /api -> http://localhost:8787
VITE_API_URL=
