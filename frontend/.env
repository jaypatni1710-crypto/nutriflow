# ================================
# NUTRIFLOW FRONTEND ENVIRONMENT
# ================================

# API Base URL - No trailing slash, no path suffix (e.g. no /api/auth)
# Local dev (wrangler dev): http://localhost:8787
# Production (deployed Worker): https://nutriflow-api.YOUR-SUBDOMAIN.workers.dev
VITE_API_URL=http://localhost:8787
